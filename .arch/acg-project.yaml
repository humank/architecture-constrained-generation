# The sample project this repository ships. `acg init` writes one of these for a real
# project; it is the only place the engine reads a project name from, so that no
# default can quietly label someone else's system "coffeeshop".
name: coffeeshop
created_at: "2026-03-19T00:00:00.000Z"
language: en
notes: >
  Sample, not a product. It deliberately ships the lies the engine exists to catch —
  see docs/acg-engine-development-plan.md §10 — so its board is red on purpose.
  To start your own project, see docs/adopting-acg.md.
